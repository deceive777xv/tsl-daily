# 每个案例使用独立 Pull Request 审批

每日制作任务为每个案例分别创建 GitHub Pull Request，案例只有在其 PR 被站点所有者合并后才获得发布批准；合并到 `main` 会立即触发 GitHub Pages 构建和发布，不再设置第二道人工门禁。相比把每日产出捆绑为一个批次，这会增加 PR 数量，但允许每个案例独立修改、接受或拒绝，同时让 `main` 分支和公开网站始终只包含获批内容。
